const i18n = {
  zh: {
    appTitle: "宠知伴 RoboPetCare",
    appDesc: "AI 驱动的宠物陪护与喂养助手，覆盖手机端、平板端和网页版演示。",
    mobile: "手机端",
    tablet: "平板端",
    web: "网页版",
    mockData: "Mock API · 双语演示数据",
    dualPrototype: "多端交互原型",
    mobileFlow: "手机端主流程",
    tabletFlow: "平板端监控与看板",
    webFlow: "网页版官网与管理台",
    mobileFlowDesc: "覆盖建档、监测、日志、异常、喂养、问答、推荐和社群。",
    tabletFlowDesc: "覆盖总览看板、多维监控、异常时间线、设备联动和演示场景切换。",
    webFlowDesc: "适合对外展示项目价值，支持网页端查看核心能力和实时状态摘要。",
    currentScenario: "当前演示场景",
    scenarioHint: "切换场景后会联动三端内容展示。",
    moodScore: "情绪得分",
    coreModules: "核心模块",
    moduleHint: "和项目 PPT 的结构保持一致。",
    m1Title: "远程监测",
    m1Desc: "实时状态、行为识别、异常提醒",
    m2Title: "AI 喂养",
    m2Desc: "喂养计划、风险提示、方案解释",
    m3Title: "智能问答",
    m3Desc: "结合当天数据给出可执行建议",
    m4Title: "推荐与社群",
    m4Desc: "产品推荐、知识内容、合作生态",
    notesTitle: "演示说明",
    notesHint: "中英文切换会立即生效，不需要刷新页面。",
    welcome: "欢迎",
    profile: "建档",
    home: "首页",
    status: "监测",
    logs: "日志",
    alert: "异常",
    feeding: "喂养",
    chat: "问答",
    shop: "推荐",
    community: "社群",
    me: "我的",
    dashboard: "总览",
    monitoring: "监控",
    timeline: "时间线",
    devices: "设备",
    demo: "演示",
    webOverview: "概览",
    webFeatures: "能力",
    webLive: "实时",
    webStories: "案例",
    statusNormal: "正常",
    statusAttention: "关注",
    statusWarning: "异常",
    statusCritical: "紧急",
    saveProfile: "保存并进入首页",
    backHome: "返回首页",
    send: "发送",
    askPlaceholder: "输入你想问的问题，例如：今天为什么喝水少了？",
    startProfile: "开始创建宠物档案",
    viewTablet: "查看平板看板",
    switchToWeb: "切换网页版",
    todaySnapshot: "今日状态摘要",
    coreEntry: "核心功能入口",
    fillProfileDone: "宠物档案已保存，系统已生成个性化喂养摘要。",
    locateLog: "已定位到对应的行为日志。",
    chooseProduct: "已将该商品加入当前演示方案。",
    scenarioChanged: "已切换演示场景：",
    needQuestion: "请先输入问题，我会结合当前演示数据回答。",
    webHeroTitle: "宠知伴网页版",
    webHeroDesc: "对外展示型页面，聚焦项目价值、核心能力与实时宠物状态。",
    openMobileDemo: "打开手机端 Demo",
    openTabletDemo: "打开平板端 Demo",
    featureTitle: "核心能力",
    realtimeTitle: "实时状态",
    storiesTitle: "场景故事",
    footerNote: "此版本为前端交互演示，后续可接入真实硬件与后端服务。"
  },
  en: {
    appTitle: "RoboPetCare",
    appDesc: "AI-powered pet care assistant demo across mobile, tablet, and web.",
    mobile: "Mobile",
    tablet: "Tablet",
    web: "Web",
    mockData: "Mock API · Bilingual Demo Data",
    dualPrototype: "Multi-Device Prototype",
    mobileFlow: "Mobile User Flow",
    tabletFlow: "Tablet Monitoring",
    webFlow: "Web Showcase & Console",
    mobileFlowDesc: "Includes profile setup, monitoring, logs, alerts, feeding, chat, recommendations, and community.",
    tabletFlowDesc: "Includes dashboard, monitoring, event timeline, device linkage, and demo scenario switching.",
    webFlowDesc: "Built for public presentation with core capabilities and real-time status summary.",
    currentScenario: "Current Scenario",
    scenarioHint: "Scenario switching updates content across all device modes.",
    moodScore: "Mood Score",
    coreModules: "Core Modules",
    moduleHint: "Aligned with your PPT product structure.",
    m1Title: "Remote Monitoring",
    m1Desc: "Live status, behavior recognition, alerts",
    m2Title: "AI Feeding",
    m2Desc: "Feeding plan, risks, explainable suggestions",
    m3Title: "AI Assistant",
    m3Desc: "Actionable advice based on live data",
    m4Title: "Recommendation & Community",
    m4Desc: "Product matching, pet knowledge, ecosystem",
    notesTitle: "Demo Notes",
    notesHint: "Language switching applies instantly without refresh.",
    welcome: "Welcome",
    profile: "Profile",
    home: "Home",
    status: "Status",
    logs: "Logs",
    alert: "Alert",
    feeding: "Feeding",
    chat: "Chat",
    shop: "Shop",
    community: "Community",
    me: "Me",
    dashboard: "Dashboard",
    monitoring: "Monitor",
    timeline: "Timeline",
    devices: "Devices",
    demo: "Demo",
    webOverview: "Overview",
    webFeatures: "Features",
    webLive: "Live",
    webStories: "Stories",
    statusNormal: "Normal",
    statusAttention: "Attention",
    statusWarning: "Warning",
    statusCritical: "Critical",
    saveProfile: "Save and Go Home",
    backHome: "Back Home",
    send: "Send",
    askPlaceholder: "Ask something, e.g. why was water intake lower today?",
    startProfile: "Create Pet Profile",
    viewTablet: "Open Tablet Dashboard",
    switchToWeb: "Open Web Version",
    todaySnapshot: "Today Snapshot",
    coreEntry: "Core Entries",
    fillProfileDone: "Profile saved. Personalized feeding summary is ready.",
    locateLog: "Related event log opened.",
    chooseProduct: "Product added to current demo package.",
    scenarioChanged: "Scenario switched: ",
    needQuestion: "Please type a question first.",
    webHeroTitle: "RoboPetCare Web",
    webHeroDesc: "A presentation-ready web experience focused on value proposition, capability highlights, and live status.",
    openMobileDemo: "Open Mobile Demo",
    openTabletDemo: "Open Tablet Demo",
    featureTitle: "Capabilities",
    realtimeTitle: "Live Status",
    storiesTitle: "Scenario Stories",
    footerNote: "This build is a front-end interaction demo. Hardware/backend integration can be added next."
  }
}

const data = {
  petProfile: {
    name: "Milo",
    breed: { zh: "英短蓝猫", en: "British Shorthair" },
    age: { zh: "2岁4个月", en: "2y 4m" },
    weight: "4.8kg",
    allergies: { zh: "乳糖敏感", en: "Lactose sensitive" },
    preference: { zh: "低敏高蛋白", en: "Hypoallergenic high-protein" },
    owner: { zh: "林晨", en: "Lynn" },
    city: { zh: "苏州", en: "Suzhou" },
    deviceName: "RPC Companion R1"
  },
  scenarios: {
    calm: {
      id: "calm",
      label: { zh: "稳定陪伴", en: "Stable Companion" },
      severity: "normal",
      mood: { zh: "放松", en: "Relaxed" },
      moodScore: 92,
      insight: {
        zh: "今日整体平稳，进食与休息节律良好。",
        en: "Overall stable today with balanced feeding and rest rhythm."
      },
      feed: 94,
      water: 88,
      activity: 74,
      rest: 91
    },
    attention: {
      id: "attention",
      label: { zh: "独处焦虑", en: "Separation Anxiety" },
      severity: "attention",
      mood: { zh: "微焦虑", en: "Mild Anxiety" },
      moodScore: 84,
      insight: {
        zh: "下午出现门口徘徊，建议远程语音安抚。",
        en: "Door pacing detected in the afternoon, remote voice soothing is recommended."
      },
      feed: 90,
      water: 78,
      activity: 66,
      rest: 80
    },
    urgent: {
      id: "urgent",
      label: { zh: "健康关注", en: "Health Concern" },
      severity: "critical",
      mood: { zh: "不适", en: "Discomfort" },
      moodScore: 63,
      insight: {
        zh: "夜间异常动作与饮水下降同时出现，需重点关注。",
        en: "Night abnormal behavior and lower water intake appeared together, needs immediate attention."
      },
      feed: 68,
      water: 54,
      activity: 48,
      rest: 57
    }
  },
  alerts: [
    {
      id: "a1",
      type: { zh: "独处焦躁预警", en: "Separation Stress Alert" },
      time: "15:20",
      severity: "attention",
      summary: {
        zh: "门口徘徊并伴随连续叫声 8 分钟。",
        en: "Door pacing with repeated meowing for 8 minutes."
      },
      suggestion: {
        zh: "建议立即语音安抚，并安排互动喂食。",
        en: "Trigger voice soothing and schedule interactive feeding."
      }
    },
    {
      id: "a2",
      type: { zh: "饮水下降提醒", en: "Hydration Drop" },
      time: "14:10",
      severity: "warning",
      summary: {
        zh: "今日饮水低于近 7 日均值 24%。",
        en: "Water intake is 24% below 7-day average."
      },
      suggestion: {
        zh: "提高湿粮比例，连续两日下降请咨询兽医。",
        en: "Increase wet food ratio; consult a vet if it persists for 2 days."
      }
    }
  ],
  products: [
    {
      id: "p1",
      name: { zh: "低敏鲜肉主粮", en: "Hypoallergenic Fresh Meat Kibble" },
      fit: "96%",
      price: "¥129",
      desc: {
        zh: "适合乳糖敏感与轻度掉毛期。",
        en: "Suitable for lactose-sensitive pets and mild shedding periods."
      }
    },
    {
      id: "p2",
      name: { zh: "功能湿粮补水包", en: "Hydration Wet Food Pack" },
      fit: "92%",
      price: "¥58",
      desc: {
        zh: "针对饮水偏低场景的补水方案。",
        en: "Hydration-focused option for low-intake days."
      }
    }
  ],
  posts: [
    {
      id: "c1",
      title: {
        zh: "上班族如何减少宠物独处焦虑",
        en: "How to reduce pet separation anxiety for office workers"
      },
      excerpt: {
        zh: "远程语音安抚 + 定时互动奖励，一周内明显改善。",
        en: "Voice soothing + scheduled interactive rewards showed clear improvement in one week."
      }
    },
    {
      id: "c2",
      title: {
        zh: "夜间异常行为先看哪三个指标",
        en: "Top 3 indicators to check for night abnormal behavior"
      },
      excerpt: {
        zh: "先看食欲、饮水和排泄，再看行为回放。",
        en: "Start with appetite, hydration, and elimination, then review behavior playback."
      }
    }
  ]
}

const state = {
  lang: "zh",
  mode: "mobile",
  mobileView: "welcome",
  tabletView: "dashboard",
  webTab: "overview",
  scenario: "attention",
  profile: {
    name: data.petProfile.name,
    breed: data.petProfile.breed.zh,
    age: data.petProfile.age.zh,
    weight: data.petProfile.weight,
    owner: data.petProfile.owner.zh
  },
  activeAlertId: "a1",
  selectedProductId: "p1",
  chatInput: "",
  chatMessages: [],
  notice: ""
}

const app = document.querySelector("#app")

function tr(key) {
  return i18n[state.lang][key] || key
}

function pick(value) {
  return typeof value === "object" ? value[state.lang] : value
}

function levelLabel(level) {
  const map = {
    normal: tr("statusNormal"),
    attention: tr("statusAttention"),
    warning: tr("statusWarning"),
    critical: tr("statusCritical")
  }
  return map[level] || level
}

function scenarioData() {
  return data.scenarios[state.scenario]
}

function activeAlert() {
  return data.alerts.find((a) => a.id === state.activeAlertId) || data.alerts[0]
}

function setNotice(text) {
  state.notice = text
}

function detectLangContent(input) {
  const lower = input.toLowerCase()
  if (state.lang === "zh") {
    if (lower.includes("焦") || lower.includes("门口")) {
      return "当前更接近轻度分离焦虑，建议先语音安抚，再补一份湿粮奖励。"
    }
    if (lower.includes("喂") || lower.includes("粮") || lower.includes("吃")) {
      return "建议少量多餐：早餐主粮、午间湿粮补水、晚间互动喂食。"
    }
    return "建议先看实时状态，再结合 AI 喂养页调整今日方案。"
  }
  if (lower.includes("anx") || lower.includes("door")) {
    return "This looks like mild separation anxiety. Trigger voice soothing and a small interactive reward feeding."
  }
  if (lower.includes("feed") || lower.includes("food") || lower.includes("eat")) {
    return "Use a small-and-frequent plan: dry food in the morning, wet food at noon, interactive feeding at night."
  }
  return "Check live status first, then adjust today's plan in the AI feeding section."
}

document.addEventListener("click", (event) => {
  const target = event.target.closest("[data-action]")
  if (!target) {
    return
  }

  const action = target.dataset.action

  if (action === "set-lang") {
    state.lang = target.dataset.lang
    render()
    return
  }

  if (action === "set-mode") {
    state.mode = target.dataset.mode
    render()
    return
  }

  if (action === "mobile-view") {
    state.mobileView = target.dataset.view
    render()
    return
  }

  if (action === "tablet-view") {
    state.tabletView = target.dataset.view
    render()
    return
  }

  if (action === "web-tab") {
    state.webTab = target.dataset.tab
    render()
    return
  }

  if (action === "set-scenario") {
    state.scenario = target.dataset.scenario
    setNotice(`${tr("scenarioChanged")}${pick(scenarioData().label)}`)
    render()
    return
  }

  if (action === "select-alert") {
    state.activeAlertId = target.dataset.alertId
    state.mobileView = "alert"
    render()
    return
  }

  if (action === "save-profile") {
    state.mobileView = "home"
    setNotice(tr("fillProfileDone"))
    render()
    return
  }

  if (action === "choose-product") {
    state.selectedProductId = target.dataset.productId
    setNotice(tr("chooseProduct"))
    render()
    return
  }

  if (action === "send-chat") {
    const content = state.chatInput.trim()
    if (!content) {
      setNotice(tr("needQuestion"))
      render()
      return
    }
    const stamp = new Date().toLocaleTimeString(state.lang === "zh" ? "zh-CN" : "en-US", {
      hour: "2-digit",
      minute: "2-digit",
      hour12: false
    })
    state.chatMessages.push({ from: "user", text: content, time: stamp })
    state.chatMessages.push({ from: "assistant", text: detectLangContent(content), time: stamp })
    state.chatInput = ""
    render()
  }
})

document.addEventListener("input", (event) => {
  const target = event.target
  if (target.dataset.profileField) {
    state.profile[target.dataset.profileField] = target.value
    return
  }
  if (target.dataset.chatInput) {
    state.chatInput = target.value
  }
})

function render() {
  const scenario = scenarioData()

  app.innerHTML = `
    <div class="app-shell">
      <header class="topbar">
        <div class="brand-cluster">
          <div class="brand-mark" aria-hidden="true"></div>
          <div class="brand-copy">
            <h1>${tr("appTitle")}</h1>
            <p>${tr("appDesc")}</p>
          </div>
        </div>
        <div class="toolbar">
          <div class="mode-toggle" aria-label="mode">
            <button class="${state.mode === "mobile" ? "active" : ""}" data-action="set-mode" data-mode="mobile">${tr("mobile")}</button>
            <button class="${state.mode === "tablet" ? "active" : ""}" data-action="set-mode" data-mode="tablet">${tr("tablet")}</button>
            <button class="${state.mode === "web" ? "active" : ""}" data-action="set-mode" data-mode="web">${tr("web")}</button>
          </div>
          <div class="mode-toggle" aria-label="language">
            <button class="${state.lang === "zh" ? "active" : ""}" data-action="set-lang" data-lang="zh">中文</button>
            <button class="${state.lang === "en" ? "active" : ""}" data-action="set-lang" data-lang="en">EN</button>
          </div>
          <div class="toolbar-pill">${tr("mockData")}</div>
        </div>
      </header>

      <main class="experience">
        <section class="preview-card">
          <div class="preview-header">
            <div class="preview-copy">
              <p class="eyebrow">${tr("dualPrototype")}</p>
              <h2>${state.mode === "mobile" ? tr("mobileFlow") : state.mode === "tablet" ? tr("tabletFlow") : tr("webFlow")}</h2>
              <p>${state.mode === "mobile" ? tr("mobileFlowDesc") : state.mode === "tablet" ? tr("tabletFlowDesc") : tr("webFlowDesc")}</p>
            </div>
            <div class="segmented">
              ${state.mode === "mobile" ? renderMobileSegmented() : state.mode === "tablet" ? renderTabletSegmented() : renderWebSegmented()}
            </div>
          </div>

          <div class="device-stage">
            ${state.mode === "mobile" ? renderMobile() : state.mode === "tablet" ? renderTablet() : renderWeb()}
          </div>
        </section>

        <aside class="context-panel">
          <section>
            <div class="panel-head">
              <div>
                <h3 class="panel-title">${tr("currentScenario")}</h3>
                <p class="panel-subtitle">${tr("scenarioHint")}</p>
              </div>
            </div>
            <div class="context-summary">
              <div class="pill-row">
                <span class="pill" data-level="${scenario.severity}">${pick(scenario.label)}</span>
                <span class="mini-pill">${tr("moodScore")} ${scenario.moodScore}</span>
              </div>
              <p class="muted">${pick(scenario.insight)}</p>
            </div>
            <div class="chip-row">
              ${Object.values(data.scenarios).map((item) => `
                <button class="nav-chip ${state.scenario === item.id ? "active" : ""}" data-action="set-scenario" data-scenario="${item.id}">${pick(item.label)}</button>
              `).join("")}
            </div>
          </section>

          <section>
            <div class="panel-head">
              <div>
                <h3 class="panel-title">${tr("coreModules")}</h3>
                <p class="panel-subtitle">${tr("moduleHint")}</p>
              </div>
            </div>
            <div class="context-list">
              <div class="context-item"><div class="status-icon">01</div><div><strong>${tr("m1Title")}</strong><span class="muted">${tr("m1Desc")}</span></div></div>
              <div class="context-item"><div class="status-icon">02</div><div><strong>${tr("m2Title")}</strong><span class="muted">${tr("m2Desc")}</span></div></div>
              <div class="context-item"><div class="status-icon">03</div><div><strong>${tr("m3Title")}</strong><span class="muted">${tr("m3Desc")}</span></div></div>
              <div class="context-item"><div class="status-icon">04</div><div><strong>${tr("m4Title")}</strong><span class="muted">${tr("m4Desc")}</span></div></div>
            </div>
          </section>

          <section>
            <div class="panel-head"><div><h3 class="panel-title">${tr("notesTitle")}</h3><p class="panel-subtitle">${state.notice || tr("notesHint")}</p></div></div>
            <div class="empty-note">${tr("notesHint")}</div>
          </section>
        </aside>
      </main>
    </div>
  `
}

function renderMobileSegmented() {
  const items = ["welcome", "profile", "home", "status", "feeding", "chat"]
  return items.map((view) => `<button class="${state.mobileView === view ? "active" : ""}" data-action="mobile-view" data-view="${view}">${tr(view)}</button>`).join("")
}

function renderTabletSegmented() {
  const items = ["dashboard", "monitoring", "timeline", "devices", "demo"]
  return items.map((view) => `<button class="${state.tabletView === view ? "active" : ""}" data-action="tablet-view" data-view="${view}">${tr(view)}</button>`).join("")
}

function renderWebSegmented() {
  const items = ["webOverview", "webFeatures", "webLive", "webStories"]
  return items.map((tab) => `<button class="${state.webTab === tab ? "active" : ""}" data-action="web-tab" data-tab="${tab}">${tr(tab)}</button>`).join("")
}

function renderMobile() {
  return `
    <div class="device-frame phone">
      <div class="phone-notch"></div>
      <div class="device-screen">
        <div class="phone-status"><strong>09:41</strong><span>${data.petProfile.deviceName}</span></div>
        <div class="mobile-app">
          <div class="mobile-content">${renderMobileScreen()}</div>
          ${state.mobileView === "welcome" || state.mobileView === "profile" ? "" : renderBottomNav()}
        </div>
      </div>
    </div>
  `
}

function renderMobileScreen() {
  const scenario = scenarioData()
  const alert = activeAlert()

  if (state.mobileView === "welcome") {
    return `
      <div class="page-stack">
        <section class="hero-card">
          <div class="hero-meta">
            <div>
              <p class="eyebrow">${tr("todaySnapshot")}</p>
              <h3>${pick(scenario.label)}</h3>
              <p>${pick(scenario.insight)}</p>
            </div>
            <div class="hero-score"><strong>${scenario.moodScore}</strong><span>${tr("moodScore")}</span></div>
          </div>
          <div class="hero-actions" style="margin-top:14px;">
            <button class="pill-button primary" data-action="mobile-view" data-view="profile">${tr("startProfile")}</button>
            <button class="pill-button secondary" data-action="set-mode" data-mode="tablet">${tr("viewTablet")}</button>
            <button class="pill-button secondary" data-action="set-mode" data-mode="web">${tr("switchToWeb")}</button>
          </div>
        </section>
      </div>
    `
  }

  if (state.mobileView === "profile") {
    return `
      <div class="page-stack">
        <section class="soft-card">
          <div class="form-grid two">
            ${renderField("name", state.profile.name)}
            ${renderField("breed", state.profile.breed)}
            ${renderField("age", state.profile.age)}
            ${renderField("weight", state.profile.weight)}
            ${renderField("owner", state.profile.owner)}
          </div>
          <div class="profile-actions" style="margin-top:14px;"><button class="pill-button primary" data-action="save-profile">${tr("saveProfile")}</button></div>
        </section>
      </div>
    `
  }

  if (state.mobileView === "home") {
    return `
      <div class="page-stack">
        <section class="hero-card">
          <div class="hero-meta">
            <div>
              <p class="eyebrow">${tr("todaySnapshot")}</p>
              <h3>${state.profile.name} · ${pick(scenario.mood)}</h3>
              <p>${pick(scenario.insight)}</p>
            </div>
            <div class="hero-score"><strong>${scenario.moodScore}</strong><span>${tr("moodScore")}</span></div>
          </div>
        </section>

        <section class="status-row">
          ${metric(tr("feeding"), `${scenario.feed}%`)}
          ${metric("Hydro", `${scenario.water}%`)}
          ${metric("Act", `${scenario.activity}%`)}
          ${metric("Rest", `${scenario.rest}%`)}
        </section>

        <button class="status-banner" data-level="${alert.severity}" data-action="select-alert" data-alert-id="${alert.id}">
          <div class="section-head"><div><h3 class="section-title">${pick(alert.type)}</h3><p class="section-subtitle">${pick(alert.summary)}</p></div><span class="mini-pill">${alert.time}</span></div>
        </button>
      </div>
    `
  }

  if (state.mobileView === "status") {
    return `
      <div class="page-stack">
        <section class="live-panel"><span class="live-badge">Live</span><div class="monitor-pet"></div><div class="overlay-tag top">${pick(scenario.mood)}</div></section>
        <section class="soft-card"><p class="muted">${pick(scenario.insight)}</p></section>
      </div>
    `
  }

  if (state.mobileView === "feeding") {
    return `
      <div class="page-stack">
        <section class="soft-card">
          <h3 class="section-title">${tr("feeding")}</h3>
          <div class="line-list">
            <div class="line-item"><strong>07:30</strong><p>${state.lang === "zh" ? "低敏主粮 + 温水" : "Hypoallergenic kibble + warm water"}</p></div>
            <div class="line-item"><strong>12:30</strong><p>${state.lang === "zh" ? "补水湿粮" : "Hydration wet food"}</p></div>
            <div class="line-item"><strong>18:40</strong><p>${state.lang === "zh" ? "互动喂食" : "Interactive feeding"}</p></div>
          </div>
        </section>
      </div>
    `
  }

  if (state.mobileView === "chat") {
    return `
      <div class="page-stack">
        <section class="chat-card">
          <div class="message-list">
            ${state.chatMessages.map((m) => `<div class="message ${m.from}"><div class="sender">${m.from === "assistant" ? "AI" : "ME"}</div><div><div class="bubble">${m.text}</div><div class="stamp">${m.time}</div></div></div>`).join("")}
          </div>
          <div class="chat-composer">
            <textarea data-chat-input="true" placeholder="${tr("askPlaceholder")}">${state.chatInput}</textarea>
            <button class="pill-button primary" data-action="send-chat">${tr("send")}</button>
          </div>
        </section>
      </div>
    `
  }

  if (state.mobileView === "shop") {
    return `
      <div class="page-stack">
        ${data.products.map((p) => `
          <article class="product-card">
            <header><strong>${pick(p.name)}</strong><span class="mini-pill">${p.fit}</span></header>
            <p>${pick(p.desc)}</p>
            <div class="pill-row"><span class="mini-pill">${p.price}</span><button class="pill-button ${state.selectedProductId === p.id ? "primary" : "secondary"}" data-action="choose-product" data-product-id="${p.id}">${tr("shop")}</button></div>
          </article>
        `).join("")}
      </div>
    `
  }

  if (state.mobileView === "community") {
    return `
      <div class="page-stack">
        ${data.posts.map((p) => `<article class="community-card"><strong>${pick(p.title)}</strong><p>${pick(p.excerpt)}</p></article>`).join("")}
      </div>
    `
  }

  if (state.mobileView === "alert") {
    return `
      <div class="page-stack">
        <section class="hero-card"><h3>${pick(alert.type)}</h3><p>${pick(alert.summary)}</p><p class="muted">${pick(alert.suggestion)}</p></section>
      </div>
    `
  }

  return `<div class="page-stack"><section class="soft-card"><p>${tr("backHome")}</p></section></div>`
}

function renderBottomNav() {
  const items = ["home", "feeding", "chat", "community", "shop"]
  return `<nav class="mobile-bottom-nav">${items.map((v) => `<button class="bottom-item ${state.mobileView === v ? "active" : ""}" data-action="mobile-view" data-view="${v}"><strong>${v[0].toUpperCase()}</strong><span>${tr(v)}</span></button>`).join("")}</nav>`
}

function renderField(field, value) {
  return `<div class="field"><label>${tr(field)}</label><input data-profile-field="${field}" value="${value}"></div>`
}

function metric(label, value) {
  return `<div class="metric-card"><span>${label}</span><strong>${value}</strong></div>`
}

function renderTablet() {
  const scenario = scenarioData()
  const alert = activeAlert()
  return `
    <div class="device-frame tablet">
      <div class="device-screen">
        <div class="tablet-shell">
          <aside class="tablet-sidebar">
            <div class="tablet-brand"><h3>${tr("tablet")}</h3><p>${pick(scenario.label)}</p></div>
            <nav class="sidebar-nav">${["dashboard", "monitoring", "timeline", "devices", "demo"].map((v) => `<button class="${state.tabletView === v ? "active" : ""}" data-action="tablet-view" data-view="${v}">${tr(v)}</button>`).join("")}</nav>
          </aside>
          <section class="tablet-main">
            ${state.tabletView === "dashboard" ? tabletDashboard(scenario, alert) : ""}
            ${state.tabletView === "monitoring" ? tabletMonitoring(scenario) : ""}
            ${state.tabletView === "timeline" ? tabletTimeline() : ""}
            ${state.tabletView === "devices" ? tabletDevices() : ""}
            ${state.tabletView === "demo" ? tabletDemo(scenario) : ""}
          </section>
        </div>
      </div>
    </div>
  `
}

function tabletDashboard(s, a) {
  return `
    <header class="tablet-topbar"><h2>${tr("dashboard")}</h2><span class="pill" data-level="${s.severity}">${pick(s.label)}</span></header>
    <section class="tablet-stats">${metric(tr("moodScore"), s.moodScore)}${metric(tr("feeding"), `${s.feed}%`)}${metric("Hydro", `${s.water}%`)}${metric("Act", `${s.activity}%`)}</section>
    <section class="tablet-grid"><article class="tablet-panel"><h3 class="panel-title">${tr("realtimeTitle")}</h3><p>${pick(s.insight)}</p></article><article class="tablet-panel"><h3 class="panel-title">${pick(a.type)}</h3><p>${pick(a.summary)}</p></article></section>
  `
}

function tabletMonitoring(s) {
  return `<section class="tablet-panel"><div class="live-panel" style="min-height:320px;"><span class="live-badge">Live</span><div class="monitor-pet" style="width:220px;height:220px"></div><div class="overlay-tag top">${pick(s.mood)}</div></div></section>`
}

function tabletTimeline() {
  return `<section class="tablet-panel">${data.alerts.map((a) => `<div class="timeline-item" data-level="${a.severity}"><div class="timeline-row"><strong>${a.time}</strong><span>${levelLabel(a.severity)}</span></div><p>${pick(a.summary)}</p></div>`).join("")}</section>`
}

function tabletDevices() {
  return `<section class="device-grid"><article class="device-card"><strong>R1 Robot</strong><p>Camera + Voice + Movement online</p></article><article class="device-card"><strong>Smart Feeder</strong><p>Connected and synchronized</p></article></section>`
}

function tabletDemo(s) {
  return `<section class="tablet-panel"><h3 class="panel-title">${tr("demo")}</h3><p>${pick(s.insight)}</p><div class="chip-row">${Object.values(data.scenarios).map((v) => `<button class="nav-chip ${state.scenario === v.id ? "active" : ""}" data-action="set-scenario" data-scenario="${v.id}">${pick(v.label)}</button>`).join("")}</div></section>`
}

function renderWeb() {
  const s = scenarioData()
  const alert = activeAlert()
  return `
    <div class="web-shell">
      <section class="web-hero">
        <div>
          <p class="eyebrow">WEB EXPERIENCE</p>
          <h2>${tr("webHeroTitle")}</h2>
          <p>${tr("webHeroDesc")}</p>
          <div class="hero-actions" style="margin-top:14px;">
            <button class="pill-button primary" data-action="set-mode" data-mode="mobile">${tr("openMobileDemo")}</button>
            <button class="pill-button secondary" data-action="set-mode" data-mode="tablet">${tr("openTabletDemo")}</button>
          </div>
        </div>
        <div class="hero-score"><strong>${s.moodScore}</strong><span>${tr("moodScore")}</span></div>
      </section>

      <section class="web-grid">
        <article class="web-card">
          <h3>${tr("featureTitle")}</h3>
          <div class="line-list">
            <div class="line-item"><strong>${tr("m1Title")}</strong><p>${tr("m1Desc")}</p></div>
            <div class="line-item"><strong>${tr("m2Title")}</strong><p>${tr("m2Desc")}</p></div>
            <div class="line-item"><strong>${tr("m3Title")}</strong><p>${tr("m3Desc")}</p></div>
          </div>
        </article>
        <article class="web-card">
          <h3>${tr("realtimeTitle")}</h3>
          <div class="pill-row"><span class="pill" data-level="${s.severity}">${pick(s.label)}</span><span class="mini-pill">${levelLabel(alert.severity)}</span></div>
          <p class="muted">${pick(alert.summary)}</p>
        </article>
      </section>

      <section class="web-card">
        <h3>${tr("storiesTitle")}</h3>
        <div class="community-grid">${data.posts.map((p) => `<article class="community-card"><strong>${pick(p.title)}</strong><p>${pick(p.excerpt)}</p></article>`).join("")}</div>
      </section>

      <section class="web-footer-note">${tr("footerNote")}</section>
    </div>
  `
}

render()
